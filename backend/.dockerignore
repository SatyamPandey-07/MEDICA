**/__pycache__
**/*.pyc
**/*.pyo
.git
.github
.env
.venv
venv
*.egg-info
.pytest_cache
tests/
# Only the markdown knowledge-base data is excluded (docker-compose bind-mounts
# it at runtime) — knowledge/*.py (graph.py, store.py, seeder.py) is source code
# and must ship in the image.
knowledge/cancers/
knowledge/entities/
**/.DS_Store
