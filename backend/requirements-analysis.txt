# MEDICA Multi-Layer Analysis Pipeline — additional dependencies
# Installed only in the Docker image (see Dockerfile), NOT in the base
# requirements.txt used by the Vercel serverless backend deployment, since
# these packages (particularly torch/transformers) exceed Vercel's function
# size limit. Code in backend/analysis/ imports all of these lazily, so the
# base app still boots fine without this file installed — only the
# comparison endpoint and paper-verification pipeline require it.

# PDF + OCR extraction
pymupdf>=1.23.0
pytesseract>=0.3.10
pillow>=10.0.0

# Data handling
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Keyword extraction
rake-nltk>=1.0.6
yake>=0.4.8
keybert>=0.8.3
nltk>=3.8.1

# Topic classification + entity/keyword NLP
torch>=2.0.0
transformers>=4.35.0
accelerate>=0.26.0
spacy>=3.7.0
pytextrank>=3.3.0

# Claims / entities LLM provider (Groq)
groq>=0.4.0
